/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['framerusercontent.com','i.ytimg.com'], // Add any domain(s) you want to allow for images
      },
};

export default nextConfig;
